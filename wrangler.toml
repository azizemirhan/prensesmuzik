# Cloudflare Workers — prensesmuzik (statik site)
# Deploy: npx wrangler deploy

name = "prensesmuzik"
compatibility_date = "2024-01-01"

# Worker ile statik dosyaları sun (HTML, CSS, img, videos vb.)
[assets]
directory = "."
